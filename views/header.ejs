<!DOCTYPE html>
<html lang="es">
  <head>
    <title><%= title %></title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
    <body class="w-100 p-0">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">
                    <img src="images/logo.png" class="" alt="Logo Offertify" width="70" class="d-inline-block align-text-top">
                    Offertify
                </a>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <% if (user.group === "user") { %>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="#">Explorar Ofertas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="#">Mis Reservas</a>
                    </li>
                    <% } %>
                    <% if (user.group === "owner") { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Acciones
                        </a>
                        <ul class="dropdown-menu active">
                        <li><a class="dropdown-item" href="#">Ofertas Activas</a></li>
                        <li><a class="dropdown-item" href="#">Añadir Oferta</a></li>
                        <li><a class="dropdown-item" href="#">Chats</a></li>
                        <li><a class="dropdown-item" href="#">Histórico</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="#">Editar Sitio</a>
                    </li>
                    <% } %>
                    <% if (user.group === "admin") { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="interfaz_admin"> interfaz admin</a>
                        </li>
                    <% } %>
                </ul>
                <span class="navbar-text">Bienvenido <%= user.name %> xD </span>
            </div>
        </nav>